# dependencies
node_modules/

# build output
dist/
.astro/

# BMad tooling — installed locally, not part of the product.
# NOTE: _bmad-output/ is deliberately NOT ignored — the PRD, architecture
# spine, SPEC and UX spines are product documentation worth versioning.
.claude/
_bmad/

# env & editor
.env
.env.*
.DS_Store
Thumbs.db
